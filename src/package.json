{
  "name": "hackthon-guarulhos",
  "version": "1.0.0",
  "description": "Totem de Autoatendimento - Prefeitura de Guarulhos",
  "type": "module",
  "main": "routes/server.js",
  "scripts": {
    "build": "node ../../node_modules/typescript/bin/tsc -p tsconfig.json",
    "dev": "node routes/server.js",
    "start": "node routes/server.js",
    "migrate": "node -e \"import('./migrations/index.js').then(({ runMigrations }) => runMigrations()).then(() => console.log('[migrations] concluidas')).catch((error) => { console.error('[migrations] falha:', error); process.exit(1); })\"",
    "check:db": "node -e \"import('./config/database.js').then(async ({ checkDatabaseConnection, closeDatabase }) => { await checkDatabaseConnection(); await closeDatabase(); })\""
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.21.0"
  }
}
